import 'package:flutter/material.dart';

class NumberIdentificationForm extends StatelessWidget {
  const NumberIdentificationForm({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('_NumberIdentificationForm'),
    );
  }
}
